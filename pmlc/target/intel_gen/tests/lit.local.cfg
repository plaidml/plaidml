import os
import platform
import subprocess

vkprobe = os.path.join('pmlc/runtime/vulkan/vkprobe')
if platform.system() == 'Windows':
    vkprobe += '.exe'

proc = subprocess.run(vkprobe)
if proc.returncode:
    config.unsupported = True
