# Bazel Workspace for Vertex.AI
workspace(name="vertexai_plaidml")

load("//bzl:boost.bzl", "with_boost")
with_boost()

load("//bzl:easylogging.bzl", "with_easylogging")
with_easylogging()

load("//bzl:coinlp.bzl", "with_coinlp")
with_coinlp()

load("//bzl:gflags.bzl", "with_gflags")
with_gflags()

load("//bzl:gmock.bzl", "with_gmock")
with_gmock()

load("//bzl:half.bzl", "with_half")
with_half()

load("//bzl:jsoncpp.bzl", "with_jsoncpp")
with_jsoncpp()

load("//bzl:minizip.bzl", "with_minizip")
with_minizip()

load("//bzl:opencl_headers.bzl", "with_opencl_headers")
with_opencl_headers()

load("//bzl:opencl_icd_loader.bzl", "with_opencl_icd_loader")
with_opencl_icd_loader()

load("//bzl:plaidml.bzl", "with_plaidml")
with_tile()

load("//bzl:protobuf.bzl", "with_protobuf")
with_protobuf()

load("//bzl:zlib.bzl", "with_zlib")
with_zlib()

# required by protobuf_python
load("//bzl:six.bzl", "with_six")
with_six()
